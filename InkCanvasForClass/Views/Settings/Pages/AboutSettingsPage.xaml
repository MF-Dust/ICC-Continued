<UserControl x:Class="Ink_Canvas.Views.Settings.Pages.AboutSettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:Ink_Canvas.Views.Settings.Pages"
             xmlns:vm="clr-namespace:Ink_Canvas.ViewModels.Settings"
             mc:Ignorable="d"
             d:DesignHeight="1200" d:DesignWidth="600"
             Background="Transparent"
             d:DataContext="{d:DesignInstance vm:AboutSettingsViewModel, IsDesignTimeCreatable=True}">

    <ui:DynamicScrollViewer VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">
        <StackPanel Margin="0,0,12,24">
            <ui:TextBlock Text="关于" FontTypography="Title" Margin="0,0,0,8"/>
            <ui:TextBlock Text="版本信息与开源许可" FontTypography="Body" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,24"/>

            <!-- UI Banner Placeholder -->
            <Border CornerRadius="8" Margin="0,0,0,24" Height="200" ClipToBounds="True">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1E3A8A" Offset="0"/>
                        <GradientStop Color="#1E1B4B" Offset="0.5"/>
                        <GradientStop Color="#312E81" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <!-- Futuristic Grid Pattern Overlay -->
                    <Grid Opacity="0.3">
                        <Grid.Background>
                            <DrawingBrush TileMode="Tile" Viewport="0,0,20,20" ViewportUnits="Absolute">
                                <DrawingBrush.Drawing>
                                    <GeometryDrawing>
                                        <GeometryDrawing.Geometry>
                                            <RectangleGeometry Rect="0,0,20,20"/>
                                        </GeometryDrawing.Geometry>
                                        <GeometryDrawing.Pen>
                                            <Pen Brush="#60A5FA" Thickness="0.5"/>
                                        </GeometryDrawing.Pen>
                                    </GeometryDrawing>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Grid.Background>
                    </Grid>

                    <!-- Content -->
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ui:TextBlock Text="{Binding ApplicationName}"
                                   FontTypography="TitleLarge"
                                   FontWeight="Bold"
                                   Foreground="#F0F9FF"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                        <ui:TextBlock Text="{Binding Version, StringFormat='Version:{0}'}"
                                   FontTypography="Subtitle"
                                   Foreground="#93C5FD"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,4"/>
                        <ui:TextBlock Text="{Binding CodeName, StringFormat='Codename:{0}'}"
                                   FontTypography="Body"
                                   Foreground="#DBEAFE"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap"
                                   MaxWidth="400"/>
                    </StackPanel>

                    <!-- Glowing Accent Lines -->
                    <Border BorderBrush="#60A5FA"
                            BorderThickness="1"
                            CornerRadius="8"
                            Opacity="0.5"
                            Margin="2"/>
                </Grid>
            </Border>

            <!-- Version Info -->
            <StackPanel Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <ui:TextBlock Text="{Binding Version, StringFormat='Release Version: {0}'}" FontTypography="Subtitle" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <ui:TextBlock Text="{Binding BundleVersion, StringFormat='Bundle Version: {0}'}" FontTypography="Subtitle" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>


            <!-- License Box -->
            <ui:Card Margin="0,0,0,24">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                        <ui:SymbolIcon Symbol="Certificate24" FontSize="24" Margin="0,0,12,0"/>
                        <StackPanel>
                            <ui:TextBlock Text="{Binding LicenseDescription}" FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                            <ui:TextBlock Text="{Binding LicenseName}" FontTypography="Subtitle" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                    <ui:TextBlock Text="{Binding LicenseDescription}"
                               FontTypography="Body"
                               TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextAlignment="Center"/>
                </StackPanel>
            </ui:Card>

            <!-- Acknowledgment -->
            <StackPanel Margin="0,0,0,16">
                <ui:TextBlock Text="致谢：" FontTypography="Subtitle" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBlock>
                    <Run Text="InkCanvasForClass" FontWeight="Bold"/>
                    <Hyperlink NavigateUri="{Binding OriginalProjectUrl}" Foreground="#60A5FA" RequestNavigate="OnHyperlinkRequestNavigate">
                        <Run Text="{Binding OriginalProjectUrl, Mode=OneWay}"/>
                    </Hyperlink>
                </TextBlock>
            </StackPanel>

        </StackPanel>
    </ui:DynamicScrollViewer>
</UserControl>
